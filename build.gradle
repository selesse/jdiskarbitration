plugins {
    id 'java'
}

group = 'com.selesse'
version = '1.0.0'

subprojects {
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(17)
        }
    }
}
